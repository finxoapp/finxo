name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type check
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Unit tests
        run: pnpm test

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium

      - name: E2E tests
        run: pnpm test:e2e
        env:
          # Real secrets when set; safe dummy fallbacks for CI redirect/render tests
          # (e2e suite does not perform actual Google OAuth flows)
          AUTH_SECRET: ${{ secrets.AUTH_SECRET || 'ci-e2e-placeholder-not-for-production' }}
          AUTH_GOOGLE_ID: ${{ secrets.AUTH_GOOGLE_ID }}
          AUTH_GOOGLE_SECRET: ${{ secrets.AUTH_GOOGLE_SECRET }}
          AUTH_ALLOWED_DOMAIN: ${{ secrets.AUTH_ALLOWED_DOMAIN || 'example.com' }}
          AUTH_URL: http://localhost:3000
